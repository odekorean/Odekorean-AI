// This file is a hand-written placeholder.
// Once your Supabase project is live, regenerate real types with:
//
//   npx supabase gen types typescript --project-id <your-project-ref> > src/types/database.ts
//
// Keeping `Database = any` here lets the rest of the codebase compile
// against `createClient<Database>()` before that step is done.
export type Database = any;
